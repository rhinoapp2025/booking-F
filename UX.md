# UX / UI — เว็บจองโรงแรม (`nail-F-main`)

แนวทางนี้สรุปสไตล์ที่ใช้อยู่จริง ไม่ใช่ธีมใหม่  
เป้าหมาย: **ดูสบายตา ใช้ง่ายบนมือถือ** โดยไม่ต้องอธิบายคู่มือ

แหล่ง token: `src/style.css`  
ธีมต่อสาขา: แอดมินแท็บ **UI** ใน `/admin/config` → CSS variables บน `:root`  
ธีมหน้ารวม `/`: แท็บ **หน้ารวม** ใน `/admin/platform` (super admin) — ไม่ดึงสีจากสาขาที่เพิ่งเข้า

ไฟล์นี้เป็นของเว็บจองเท่านั้น ไม่ใช้กับคีออสก์ที่ root

---

## หลักคิด (ทำไมถึงใช้ง่าย)

1. **มือถือก่อน** — คอลัมน์แคบ อ่านจบในหนึ่งนิ้ว ปุ่มใหญ่พอแตะ
2. **หนึ่งหน้า หนึ่งงาน** — จอง / จ่าย / แชท / แอดมิน แยกชัด ไม่ยัดทุกอย่างในหน้าเดียว
3. **สถานะเห็นทันที** — ป้ายเม็ดยา (pill) สีอ่อน + คำไทยสั้น ไม่พึ่งไอคอนอย่างเดียว
4. **การ์ดเป็นหน่วยข้อมูล** — แต่ละจอง / ห้อง / สถิติอยู่ในการ์ดขาว มองเป็นก้อน ไม่ใช่ตารางแน่น
5. **คำไทยตรงไปตรงมา** — «จองวันนี้» «ยกเลิก» «เช็คอินแล้ว» ไม่ใช้ศัพท์ระบบกับแขก
6. **สีนavy มืออาชีพ** — น้ำเงินเข้ม / ทอง / พื้นเทาอ่อน โทน Rhino ไม่ใช่ชมพูตุ่นหรือครีม

อย่าทำให้ «หรู» จนอ่านยาก และอย่าทำให้ «แออัด» แบบแดชบอร์ดองค์กร

---

## เลย์เอาต์

| จุดตัด | พฤติกรรม |
|--------|----------|
| ค่าเริ่ม | คอลัมน์กลาง `--page-max-width: 430px` เหมือนแอปในมือ |
| ≥ 431px | พื้นหลังรอบๆ เป็น `--color-surface-muted` ตัวแอปลอยเป็นแผ่น |
| ≥ 900px | เมนูล่างกลายเป็น **ไซด์บาร์ซ้าย 240px** เนื้อหาขยาย |
| แอดมิน | กว้างขึ้น (`max-width` ~1200px) เพราะมีแท็บและกริดห้อง |
| ล็อกอิน | การ์ดกลางจอ `max-width` ~420px เต็มความกว้างมือถือ มี safe area — ไม่ชิดซ้ายบนเดสก์ท็อป |
| ค้นหารวม `/` | เลย์เอาต์เดียวกับหน้าจองสาขา: หัวข้อจากแบรนด์หน้ารวม (ชื่อ/โลโก้/รูปปก/ธีม) + แท็บค้นหา/การจองของฉัน + ฟอร์มวัน/จังหวัด/คน + การ์ดโรงแรมแบบการ์ดห้อง |

โครงสร้างหน้าแขกที่ใช้ซ้ำ:

- `.app-page` / `.app-page--nav` — เผื่อความสูงเมนูล่าง + safe area
- `.app-header` — ติดบน โปร่ง + blur ไม่บังเนื้อหาตอนเลื่อน
- `.app-content` — ช่องว่างแนวตั้ง `--space-3` ระหว่างก้อน
- `BottomNav` — ไอคอน Tabler + ป้ายไทยสั้น; รายการที่ active ทา `--color-primary-light`

padding ซ้ายขวาใช้ `--page-padding-x` เท่านั้น อย่าใส่ `20px` กระจัดกระจาย

แอดมิน: แท็บเม็ดยาเลื่อนแนวนอน (`.tab-btn`) ไม่ใช้เมนูซ้อนหลายชั้น

---

## สี

ค่าเริ่มต้น (สาขายังไม่ตั้งธีม):

| บทบาท | ตัวแปร | ค่า |
|--------|--------|-----|
| พื้นหน้า | `--color-background` | `#F0F2F5` |
| การ์ด | `--color-surface-elevated` | `#FFFFFF` |
| เส้นขอบ | `--color-border` | `#DCE1E8` |
| ตัวอักษร | `--color-text-primary` | `#1A2332` |
| คำอธิบาย | `--color-text-secondary` / `--color-text-muted` | `#5A6577` / `#8B95A5` |
| ปุ่มหลัก | `--color-primary` | `#001529` |
| เน้นทอง | `--color-accent` | `#C9A227` |
| สำเร็จ / เตือน / ผิดพลาด | `--color-success` / `--warning` / `--error` | เขียว / ทอง / แดง (โทนแดชบอร์ด) |

พื้นหลังปุ่มหลักใช้ `--color-primary` ตัวอักษรขาว `--color-on-primary`  
พื้นอ่อนของสถานะใช้ `color-mix` จากสี semantic ไม่ใส่ hex ใหม่ถ้ามีตัวแปรอยู่แล้ว

**ค่าเริ่ม** เป็น navy + พื้นเทาอ่อน; ธีมสาขาเปลี่ยนได้แค่ primary / ตัวอักษร / พื้นหลัง / ฟอนต์ — หลีกเลี่ยงนีออนเป็นค่าเริ่ม

---

## ตัวอักษร

- เนื้อหา: `--font-body` (`Noto Sans Thai` หรือฟอนต์สาขา: Sarabun / Prompt / Kanit / IBM / system)
- ชื่อโรงแรมบนหน้าล็อกอิน: `--font-display` (Cormorant Garamond) เท่านั้น ไม่ใช้ serif ทั้งแอป
- ขนาดผ่าน `--text-h1` … `--text-label` และ `--font-scale` (sm/md/lg/xl จากตั้งค่าสาขา)
- น้ำหนักที่ใช้อยู่: 500 ป้าย, 600 หัวข้อ/ปุ่ม, 700 ตัวเลขสถิติและราคา
- ตัวเลขเงินและเวลานับถอยหลังใส่ `.tabular-nums`

`input` / `select` / `textarea` ต้อง **16px ขึ้นไป** เพื่อกัน iOS ซูมตอนโฟกัส

---

## รูปทรง ระยะ เงา

- ระยะฐาน **4px**: `--space-1` … `--space-8`
- มุมการ์ด `--radius-card` (18px) ปุ่ม `--radius-sm` / `--radius-md` ป้าย `--radius-pill`
- ชีตจากล่าง `--radius-sheet` มุมบนเท่านั้น
- เงาบาง `--shadow-card` / `--shadow-sm` ไม่ใช้เงาหนาดำ
- ปุ่มแตะขั้นต่ำ `--touch-min: 44px` ปุ่มหลักสูง `--btn-primary-height: 48px`

---

## คอมโพเนนต์ที่ใช้ซ้ำ

ใช้ของที่มีใน `style.css` ก่อนเขียน CSS ชุดใหม่

| งาน | คลาส / แพทเทิร์น |
|-----|-------------------|
| ก้อนข้อมูล | `.card` |
| ปุ่มหลัก | `.btn.primary` / `.btn-primary` |
| ปุ่มรอง | `.btn` / `.btn-outline` |
| อันตราย | `.btn.danger` |
| เล็กในแถวการ์ด | `.btn-sm` |
| แท็บ | `.tab-btn` + `.active` ทาสี primary |
| ว่าง / โหลด / ไม่มีข้อมูล | `.state-card` + ไอคอน Tabler กลาง |
| แจ้งสำเร็จ/ผิด | `.alert-banner.success` / `.error` / `.warning` |
| ยืนยันทำลาย | SweetAlert2 ไม่ใช่ `window.confirm` |
| เมนูบัญชี | `AccountMenuDrawer` |
| แผงจากล่าง | `.sheet-overlay` + `.sheet-panel` + `.sheet-handle` |
| ฟิลด์ล็อกอิน | `.field` โฟกัสมีวง primary จาง |
| ฟิลด์แอดมิน | `.admin-input` |

ไอคอนใช้ **Tabler** (`ti ti-…`) ขนาดประมาณ 20px ในนาวี ไม่ผสมชุดไอคอนอื่น

---

## สถานะการจอง (pill)

ป้ายสั้น มุมมน พื้นสีอ่อน คำไทย:

| `status` | ข้อความ | โทน |
|----------|---------|-----|
| `awaiting_payment` | รอชำระ | เหลืองอ่อน |
| `pending` | รอยืนยัน | ฟ้าอ่อน |
| `confirmed` | ยืนยันแล้ว | เขียวอ่อน |
| `checked_in` | เช็คอินแล้ว | ฟ้าเทา |
| `checked_out` | เช็คเอาต์แล้ว | เทา |
| `cancelled` | ยกเลิก | ชมพูอ่อน |

อย่าใส่สถานะภาษาอังกฤษให้แขก และอย่าใช้จุดสีอย่างเดียวโดยไม่มีคำ

---

## หน้าแขก — ลำดับงาน

1. **เลือกสาขา** → ล็อกอิน (รูป hero + ชื่อ + ฟอร์มสั้น หรือ LINE)
2. **จอง** — เลือกวัน/คน → ค้นหาห้องเป็นการ์ด → กรอกแขกในโมดัล/ชีต → ยืนยัน
3. **จ่าย** (ถ้าเปิดสลิป) — QR/บัญชี + อัปโหลดรูป สถานะชัด
4. **ของฉัน** — รายการการ์ด สถานะ pill ปุ่มเท่าที่ทำได้จริง (เช่น ยกเลิกได้แค่รอจ่าย)
5. **แชท / ที่ตั้ง / รีวิว** — แสดงเมื่อ feature flag เปิด ไม่โชว์เมนูว่าง

อย่าให้แขกเห็นศัพท์ PMS (`ResvNo`, SysDate) นอกจากจำเป็นในหน้าแอดมิน

---

## หน้าแอดมิน

- แท็บไม่กี่อัน: ภาพรวม / การจอง / ห้อง / PMS
- ภาพรวม: ตัวเลขใหญ่สี primary การ์ด 2 คอลัมน์ + ห้องว่างเป็นชิป ไม่ใช่กราฟ
- การจอง: การ์ดต่อใบ ไม่ใช่ตาราง Excel บนมือถือ; ปุ่มการกระทำอยู่ล่างการ์ด
- ถ้าเปิด PMS: ซ่อนปุ่มเช็คอิน/ยกเลิกบนเว็บ เขียนข้อความสั้นว่าให้ทำที่ PMS
- ตั้งค่าโรงแรมแยกหน้า `/admin/config` ไม่ยัดเข้าแท็บภาพรวม

---

## สถานะว่าง โหลด ผิดพลาด

- โหลด: `.state-card` + `ti-loader-2` หมุน ไม่ใส่สปินเนอร์เต็มจอทึบ
- ว่าง: หัวข้อสั้นหนึ่งบรรทัด เช่น «ไม่มีการจอง»
- ผิดพลาด: ข้อความไทยจาก API ใต้ฟอร์ม หรือ SweetAlert ถ้าเป็นการกระทำที่พลาด
- โครงร่าง `.skeleton` ใช้ได้ถ้าโหลดนาน — เคารพ `prefers-reduced-motion`

อย่าวาง empty state เป็นหน้าขาวไม่มีกรอบ

---

## การเคลื่อนไหว

- ค่าเริ่ม `--transition: 180ms ease-out`
- ชีต `--transition-sheet: 280ms`
- ปุ่ม `:active` ย่อ `scale(0.98)` เล็กน้อย
- ถ้าผู้ใช้ลดแอนิเมชัน ตัด transition ตามที่ `style.css` ทำอยู่แล้ว

ไม่ใส่พารัลแลกซ์ แอนิเมชันหน้าแรกยาว หรือโหลดสแปลช

---

## ภาษาและน้ำเสียง

- UI ทั้งแอปเป็น**ไทย**
- ประโยคสั้น บอกว่าเกิดอะไรและต้องทำอะไรต่อ
- ปุ่มเป็นคำกริยา: «ยืนยัน» «ยกเลิก» «อัปโหลดสลิป» ไม่ใช่ «OK» / «Submit»
- คำอธิบายรองใช้ `.muted` หรือ `--text-caption` ไม่ตะโกนเท่าหัวข้อ

---

## สิ่งที่ห้าม (เพื่อไม่ให้พังความใช้ง่าย)

- อย่าสร้างชุดสี/มุม/ฟอนต์ใหม่ในหน้าเดียว ถ้ายังใช้ token ได้
- อย่าทำตารางแน่นบนมือถือ — ใช้การ์ดหรือชิป
- อย่าซ่อนการกระทำหลักในเมนู `⋯` ถ้ามีปุ่มเดียวที่ต้องกด
- อย่าใส่ม็อดัลซ้อนม็อดัล; SweetAlert ต้องอยู่บนสุด (มีคลาส `swal-over-app-modal`)
- อย่า import หน้าคีออสก์เข้า `nail-F-main`
- อย่า hardcode แค่สาขา `default` ใน UI ใหม่
- อย่าลืม safe area และที่ว่างใต้ BottomNav

---

## ตรวจก่อนถือว่าหน้าใหม่เสร็จ

- [ ] มือถือแคบ (~390px) อ่านและแตะปุ่มหลักได้
- [ ] เดสก์ท็อป ≥ 900px นาวีเป็นไซด์บาร์ เนื้อหาไม่ชิดขอบ
- [ ] โหลด / ว่าง / error มี `.state-card` หรือแบนเนอร์
- [ ] สีมาจากตัวแปร หรือธีมสาขา ไม่มี contrast พังเมื่อเปลี่ยน primary
- [ ] ข้อความไทย สถานะเป็น pill
- [ ] ไม่บังเมนูล่างหรือ notch
